# 🌐 استخدام ngrok لجعل السيرفر المحلي متاح من Mobile Data

## 📖 ما هو ngrok؟
ngrok هو أداة تجعل السيرفر المحلي (Local Server) متاح على الإنترنت بـ URL عام.

---

## 🚀 خطوات الإعداد

### 1️⃣ تحميل ngrok

**على Windows:**
1. افتح المتصفح واذهب إلى: https://ngrok.com/download
2. حمّل النسخة الخاصة بـ Windows
3. فك ضغط الملف في مجلد (مثلاً: `C:\ngrok`)
4. افتح Command Prompt في نفس المجلد

**على Linux/Mac:**
```bash
# استخدم package manager
brew install ngrok  # Mac
# أو
sudo snap install ngrok  # Linux
```

---

### 2️⃣ تشغيل ngrok

**على جهاز السيرفر:**
```bash
# افتح Command Prompt أو Terminal
# اذهب لمجلد ngrok
cd C:\ngrok

# شغل ngrok على نفس البورت (8083)
ngrok http 8083
```

---

### 3️⃣ الحصول على Public URL

بعد تشغيل ngrok، سترى شاشة مثل:
```
ngrok                                                                     

Session Status                online
Account                       Free
Version                       3.x.x
Region                        United States (us)
Latency                       45ms
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://abc123.ngrok-free.app -> http://localhost:8083

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

**📝 انسخ الـ URL:**
```
https://abc123.ngrok-free.app
```

---

### 4️⃣ تحديث Base URL في التطبيق

**الطريقة 1: من داخل التطبيق (مباشرة)**
1. افتح التطبيق
2. شاشة Login
3. اضغط زر ⚙️ Settings (أعلى اليمين)
4. أدخل الـ URL من ngrok:
   ```
   https://abc123.ngrok-free.app
   ```
5. اضغط **Save**
6. حاول تسجيل الدخول مرة أخرى

**الطريقة 2: من الكود (دائم)**
```dart
// lib/core/services/url_config_service.dart
static const String _defaultBaseUrl = 'https://abc123.ngrok-free.app';
```

---

### 5️⃣ اختبار

**الآن جرب:**
1. أغلق WiFi
2. افتح بيانات الموبايل
3. افتح التطبيق
4. سجل دخول

**يجب أن يعمل! ✅**

---

## ⚠️ ملاحظات مهمة

### 🔴 عيوب ngrok (النسخة المجانية):

1. **الـ URL يتغير كل مرة**
   - عند إيقاف ngrok وتشغيله مرة أخرى، ستحصل على URL جديد
   - يجب تحديث Base URL في التطبيق كل مرة

2. **محدود الاستخدام**
   - النسخة المجانية لها حد للطلبات (requests)

3. **للتطوير فقط**
   - **لا تستخدمه في Production!**

---

## 🎯 الحل 1: استخدم ngrok بحساب (مجاني)

1. سجل حساب على https://ngrok.com
2. احصل على Auth Token
3. شغل:
   ```bash
   ngrok config add-authtoken YOUR_TOKEN_HERE
   ngrok http 8083
   ```

---

## 🎯 الحل 2: Static Domain (مدفوع)

**إذا كنت تريد URL ثابت:**
- اشترك في ngrok بـ $8/شهر
- احصل على Static Domain
- مثلاً: `https://myapp.ngrok.io`

---

## 💡 بدائل ngrok (مجانية)

### 1. **localtunnel**
```bash
npm install -g localtunnel
lt --port 8083
```

### 2. **serveo**
```bash
ssh -R 80:localhost:8083 serveo.net
```

### 3. **Cloudflare Tunnel** (الأفضل للإنتاج)
```bash
# تثبيت cloudflared
# ثم:
cloudflared tunnel --url http://localhost:8083
```

---

## 🏢 الحل النهائي (للإنتاج):

### الخيار 1: Public IP + Port Forwarding
1. احصل على Public IP من مزود الإنترنت
2. افتح Port Forwarding في الراوتر للبورت 8083
3. استخدم: `http://YOUR_PUBLIC_IP:8083`

### الخيار 2: استضافة السيرفر على Cloud
- AWS EC2
- Google Cloud
- DigitalOcean
- Heroku
- Railway.app

### الخيار 3: Domain Name
- اشترِ Domain (من Namecheap, GoDaddy, etc)
- اربطه بـ Public IP أو Cloud Server
- استخدم: `https://yourdomain.com`

---

## 📋 Checklist سريع

### استخدام ngrok الآن:
- [ ] حمّل ngrok
- [ ] شغل: `ngrok http 8083`
- [ ] انسخ الـ URL
- [ ] حدث Base URL في التطبيق (Settings)
- [ ] اختبر على Mobile Data

---

## 🎉 النتيجة

بعد استخدام ngrok:
- ✅ يعمل على WiFi
- ✅ يعمل على Mobile Data
- ✅ يعمل من أي مكان في العالم!

---

## 📞 مساعدة إضافية

**إذا واجهت مشاكل:**
1. تأكد أن السيرفر شغال على البورت 8083
2. تأكد أن ngrok يعمل بدون أخطاء
3. تأكد من نسخ الـ URL بشكل صحيح
4. تأكد أنك غيرت Base URL في التطبيق

**اختبر السيرفر:**
```bash
# في browser على نفس الجهاز
http://localhost:8083

# في browser على الموبايل (بعد ngrok)
https://abc123.ngrok-free.app
```
